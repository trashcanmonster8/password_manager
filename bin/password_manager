#!/usr/bin/env ruby
# frozen_string_literal: true

file_name = File.dirname(__FILE__) + '/../lib'
$LOAD_PATH.unshift(file_name) unless $LOAD_PATH.include?(file_name)

require 'password_manager'

PasswordManager::Application.start
